const Link = {
    id: (parent) => parent.id,
    url: (parent) => parent.url,
    title: (parent) => parent.title,
    description: (parent) => parent.description,
    postedBy: (parent) => parent.postedBy,
};


module.exports = Link;